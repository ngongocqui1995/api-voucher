{
    "version": 2,
    "installCommand": "yarn install && yarn build",
    "builds": [
       { "src": "dist/main.js", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "dist/main.js" }
    ]
 }