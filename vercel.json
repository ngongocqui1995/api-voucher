{
    "version": 2,
    "installCommand": "yarn install",
    "buildCommand": "yarn build",
    "outputDirectory": "dist",
    "devCommand": "yarn start:prod",
    "routes": [
       { "src": "/(.*)", "dest": "dist/main.js" }
    ]
 }