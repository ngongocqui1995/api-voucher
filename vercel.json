{
    "version": 2,
    "name": "api-voucher",
    "buildCommand": "yarn build",
    "builds": [
       { "src": "dist/main", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "dist/main" }
    ]
 }